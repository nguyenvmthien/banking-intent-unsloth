# Inference Configuration
model_name: "unsloth/llama-3-8b-Instruct-bnb-4bit" # Used for baseline comparison
model_path: "../outputs/checkpoint"              # Used for final inference
max_seq_length: 256
load_in_4bit: true
max_new_tokens: 32 # Maximum tokens to generate for the intent text
